#ifndef THREESUMPROBLEM_H
#define THREESUMPROBLEM_H

#include <vector>

using namespace std;

/// @brief Decision for 3 sum problem
/// Complexity O(n^2)
/// @param[in] vec - input digit array
/// @return arrays of element, sum of which equal to zero
vector<vector<int>> threeSumSolve(vector<int>&& vec);

#endif // THREESUMPROBLEM_H
